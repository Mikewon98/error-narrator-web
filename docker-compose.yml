services:
  watchtower:
    image: nickfedor/watchtower
    restart: unless-stopped
    command:
      - "--label-enable"
      - "--rolling-restart"
      - "--include-restarting"
      - "--stop-timeout"
      - "60s"
      - "--scope=my-canvas"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
    environment:
      - WATCHTOWER_POLL_INTERVAL=300
      - REPO_USER=mikewon98
      - REPO_PASS=${GITHUB_TOKEN}
      - WATCHTOWER_NOTIFICATIONS=shoutrrr
      - WATCHTOWER_NOTIFICATION_URL=${DISCORD_WEBHOOK_URL}
  my-canvas:
    image: ghcr.io/mikewon98/my-canvas:stable
    restart: unless-stopped
    networks:
      - portfolio_default
    labels:
      - "traefik.enable=true"

      # Main HTTPS router
      - "traefik.http.routers.my-canvas.rule=Host(`mycanvas.michaelyze.com`)"
      - "traefik.http.routers.my-canvas.entrypoints=websecure"
      - "traefik.http.routers.my-canvas.tls.certresolver=myresolver"

      # HTTP redirect router
      - "traefik.http.routers.my-canvas-http.rule=Host(`mycanvas.michaelyze.com`)"
      - "traefik.http.routers.my-canvas-http.middlewares=redirect-to-https-en"
      - "traefik.http.routers.my-canvas-http.entrypoints=web"

      # Service configuration
      - "traefik.http.services.my-canvas.loadbalancer.server.port=3000"

      # Middlewares
      - "traefik.http.middlewares.redirect-to-https-en.redirectscheme.permanent=true"
      - "traefik.http.middlewares.redirect-to-https-en.redirectscheme.scheme=https"

      # Rate limiting (protection against bots)
      - "traefik.http.middlewares.rate-limit.ratelimit.average=100"
      - "traefik.http.middlewares.rate-limit.ratelimit.burst=50"
      - "traefik.http.middlewares.rate-limit.ratelimit.period=1s"

      # Watchtower
      - "com.centurylinklabs.watchtower.enable=true"
      - "com.centurylinklabs.watchtower.scope=my-canvas"

    env_file:
      - .env

networks:
  portfolio_default:
    external: true
